#include "spriteButton.h"

void btn::SpriteButton::clicked()
{
}

void btn::SpriteButton::howered()
{
    /*if (this->sprite.getScale() < {this->maxScale.x, this->maxScale.y})
    {
        this->sprite.setScale({this->sprite.getScale().x + this->changeBy, this->sprite.getScale().y + this->changeBy});
        this->hitBox.setSize({this->text.getLocalBounds().width, static_cast<float>(this->text.getLocalBounds().height * 1.35)});
    }
    else
    {
        this->text.setCharacterSize(this->maxCharacterSize);
        this->hitBox.setSize({this->text.getLocalBounds().width, static_cast<float>(this->text.getLocalBounds().height * 1.35)});
    }*/
}
